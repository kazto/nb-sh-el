# nb.el テスト計画書

## 目的

このドキュメントは、nb.el パッケージの統合テストを実施するための計画書です。実際に nb コマンドを実行し、Emacs との統合が正しく機能することを確認します。

## テスト環境の準備

### テスト環境のセットアップ

```bash
# 1. nb.sh のバージョン確認
nb --version

# 2. テスト用ノートブックの作成
nb notebooks add test-nb-el

# 3. テスト用ノートブックに切り替え
nb use test-nb-el

# 4. サンプルノートの作成
nb add "Sample Note 1" --content "This is a test note."
nb add "Sample Note 2" --content "Another test note."
nb add "Sample Note 3" --content "Third test note."

# 5. Emacs で nb.el をロード
emacs -Q -l nb.el -eval "(nb-mode 1)"
```

---

## テストケース一覧

### 1. パッケージのロードと初期化

#### 1.1 nb-mode の有効化

**目的**: nb-mode が正常に有効化されることを確認

**手順**:
1. `M-x nb-mode RET` を実行
2. モードラインを確認

**期待結果**:
- [ ] モードラインに " nb" が表示される
- [ ] `C-c n` プレフィックスキーが有効になる

---

### 2. 基本コマンドのテスト

#### 2.1 nb (ノート一覧表示)

**目的**: ノートの一覧が正しく表示されることを確認

**手順**:
1. `C-c n RET` または `M-x nb RET` を実行
2. `*nb*` バッファが開くことを確認
3. バッファの内容を確認

**期待結果**:
- [ ] `*nb*` バッファが新しいウィンドウで開く
- [ ] テスト用ノートブック内のノート一覧が表示される
- [ ] 各ノートに `[数字]` 形式の ID が表示される
- [ ] カラー表示が適用されている
- [ ] バッファが読み取り専用モードになっている

**追加確認**:
- [ ] `q` キーでウィンドウが閉じる
- [ ] `n` キーで次の行に移動
- [ ] `p` キーで前の行に移動

#### 2.2 nb-add (ノート追加)

**目的**: 新しいノートを追加できることを確認

**手順**:
1. `C-c n a` を実行
2. プロンプトに `"Test from Emacs" --content "Created via nb.el"` と入力
3. 実行完了を確認
4. `C-c n RET` で一覧を再表示

**期待結果**:
- [ ] プロンプトが表示される: `nb add (options/content):`
- [ ] コマンドが正常に実行される
- [ ] 新しいノート "Test from Emacs" が一覧に表示される

**テストバリエーション**:
- [ ] 引数なしで実行（`C-c n a RET`）: 対話的にノート追加
- [ ] タイトルのみ指定: `"Title Only"`
- [ ] フォルダ指定: `folder/note.md --content "In folder"`

#### 2.3 nb-edit (ノート編集)

**目的**: ノートを Emacs バッファで開いて編集できることを確認

**手順**:
1. `C-c n e` を実行
2. プロンプトに `1` (または存在するノートの ID) を入力
3. ファイルが開くことを確認
4. 内容を編集して保存 (`C-x C-s`)

**期待結果**:
- [ ] プロンプトが表示される: `nb edit (id/filename):`
- [ ] ノートのファイルが Emacs バッファで開く
- [ ] ファイルパスが正しい（通常は `~/.nb/notebooks/test-nb-el/` 配下）
- [ ] ファイルを編集・保存できる

**追加テスト**:
- [ ] ファイル名で開く: `sample-note-1.md`
- [ ] 存在しないIDを指定: `999` → エラーメッセージが表示される

---

### 3. 出力バッファからのインタラクション

#### 3.1 RET でノートを開く

**目的**: 出力バッファからノートを直接開けることを確認

**手順**:
1. `C-c n RET` でノート一覧を表示
2. カーソルをノートの行に移動
3. `RET` キーを押す

**期待結果**:
- [ ] カーソル位置のノートが新しいバッファで開く
- [ ] `*nb*` バッファのウィンドウが閉じる
- [ ] 開いたファイルが編集可能

**テストケース**:
- [ ] 最初のノート (ID: 1) を開く
- [ ] 中間のノート (ID: 2) を開く
- [ ] 最後のノート (ID: 3) を開く
- [ ] ID がない行で RET を押す → エラーメッセージ表示

---

### 4. ブックマーク機能

#### 4.1 nb-bookmark (ブックマーク追加)

**目的**: URL をブックマークとして追加できることを確認

**手順**:
1. `C-c n b` を実行
2. プロンプトに `https://example.com --title "Example Site"` と入力
3. ブックマークが追加されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb bookmark (url/options):`
- [ ] ブックマークが正常に追加される
- [ ] `C-c n RET` でブックマークが一覧に表示される

**追加テスト**:
- [ ] URLのみ指定: `https://github.com`
- [ ] タグ付き: `https://example.com --tags tech,reference`
- [ ] コメント付き: `https://example.com --comment "Useful site"`

---

### 5. 検索機能

#### 5.1 nb-search (ノート検索)

**目的**: ノート内容を検索できることを確認

**手順**:
1. `C-c n s` を実行
2. プロンプトに `test` と入力
3. 検索結果が表示されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb search (query):`
- [ ] "test" を含むノートが `*nb*` バッファに表示される
- [ ] 検索キーワードがハイライトされる（nb.sh の機能）

**テストバリエーション**:
- [ ] AND検索: `test --and note`
- [ ] OR検索: `test --or sample`
- [ ] タグ検索: `--tag tech`
- [ ] 存在しないキーワード: `xyz123` → 結果なし

---

### 6. ノートブック管理

#### 6.1 nb-notebooks (ノートブック一覧)

**目的**: ノートブック一覧が表示されることを確認

**手順**:
1. `C-c n n` を実行
2. プロンプトで何も入力せずに RET

**期待結果**:
- [ ] プロンプトが表示される: `nb notebooks (options):`
- [ ] すべてのノートブック一覧が表示される
- [ ] 現在のノートブック（test-nb-el）にインジケータ（*など）がつく

#### 6.2 nb-use (ノートブック切り替え)

**目的**: ノートブックを切り替えられることを確認

**手順**:
1. `C-c n u` を実行
2. プロンプトに `home` と入力（デフォルトノートブック）
3. `C-c n RET` で一覧を確認

**期待結果**:
- [ ] プロンプトが表示される: `nb use (notebook):`
- [ ] ノートブックが切り替わる
- [ ] 切り替え後の一覧に home ノートブックの内容が表示される

---

### 7. ファイル操作

#### 7.1 nb-copy (ノートコピー)

**目的**: ノートをコピーできることを確認

**手順**:
1. `C-c n c` を実行
2. プロンプトに `1 copied-note.md` と入力
3. コピーが作成されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb copy (source dest):`
- [ ] ノートが正常にコピーされる
- [ ] `C-c n RET` でコピーされたノートが一覧に表示される

#### 7.2 nb-move (ノート移動/リネーム)

**目的**: ノートを移動またはリネームできることを確認

**手順**:
1. `C-c n m` を実行
2. プロンプトに `copied-note.md renamed-note.md` と入力
3. リネームされることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb move (source dest):`
- [ ] ノートが正常にリネームされる
- [ ] 一覧で新しいファイル名が表示される

#### 7.3 nb-delete (ノート削除)

**目的**: ノートを削除できることを確認

**手順**:
1. `C-c n d` を実行
2. プロンプトに `renamed-note.md --force` と入力
3. 削除されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb delete (id/filename):`
- [ ] ノートが削除される
- [ ] 一覧から削除されたノートが消える

**注意**: `--force` フラグを使用して確認をスキップ

---

### 8. 表示機能

#### 8.1 nb-show (ノート表示)

**目的**: ノートの内容が表示されることを確認

**手順**:
1. `C-c n w` を実行（`w` は show コマンドのキー）
2. プロンプトに `1` と入力
3. ノートの内容が表示されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb show (id/filename):`
- [ ] `*nb*` バッファにノートの内容が整形されて表示される
- [ ] メタデータ（パス、タイトルなど）が表示される

#### 8.2 nb-peek (ノートプレビュー)

**目的**: ノートのプレビューが表示されることを確認

**手順**:
1. `C-c n p` を実行
2. プロンプトに `1` と入力
3. プレビューが表示されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb peek (id/filename):`
- [ ] ノートのプレビューが `*nb*` バッファに表示される

---

### 9. TODO 機能

#### 9.1 nb-todo (TODO 追加)

**目的**: TODO を追加できることを確認

**手順**:
1. `C-c n t` を実行
2. プロンプトに `add "Test TODO" --task "Task 1" --task "Task 2"` と入力
3. TODO が追加されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb todo (add/do/undo options):`
- [ ] TODO が正常に追加される
- [ ] 一覧に TODO が表示される（チェックボックス付き）

**追加テスト**:
- [ ] タスクを完了: `do 1 1` (TODO ID 1 のタスク 1 を完了)
- [ ] タスクを未完了に: `undo 1 1`

---

### 10. フォルダ機能

#### 10.1 nb-folders (フォルダ一覧)

**目的**: フォルダ一覧が表示されることを確認

**手順**:
1. 事前にフォルダを作成: `nb folders add projects`
2. `C-c n f` を実行
3. プロンプトで RET のみ押す

**期待結果**:
- [ ] プロンプトが表示される: `nb folders (add/delete name):`
- [ ] フォルダ一覧が表示される
- [ ] 作成した "projects" フォルダが表示される

**追加テスト**:
- [ ] フォルダ追加: `add test-folder`
- [ ] フォルダ削除: `delete test-folder --force`

---

### 11. Git 統合

#### 11.1 nb-git (Git コマンド実行)

**目的**: ノートブック内で Git コマンドを実行できることを確認

**手順**:
1. `C-c n g` を実行
2. プロンプトに `status` と入力
3. Git ステータスが表示されることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb git (command):`
- [ ] Git ステータスが `*nb*` バッファに表示される

**追加テスト**:
- [ ] ログ表示: `log --oneline -5`
- [ ] 差分表示: `diff`

#### 11.2 nb-sync (同期)

**目的**: リモートリポジトリと同期できることを確認

**手順**:
1. `C-c n S` を実行（大文字の S）
2. 同期が実行されることを確認

**期待結果**:
- [ ] リモートが設定されている場合、git pull/push が実行される
- [ ] 結果が `*nb*` バッファに表示される

**注意**: リモートリポジトリが設定されていない場合はスキップ

---

### 12. その他の機能

#### 12.1 nb-import (ファイルインポート)

**目的**: 外部ファイルをインポートできることを確認

**手順**:
1. テスト用ファイルを作成: `echo "Imported content" > /tmp/test-import.txt`
2. `C-c n i` を実行
3. プロンプトに `/tmp/test-import.txt` と入力

**期待結果**:
- [ ] プロンプトが表示される: `nb import (path/url):`
- [ ] ファイルがノートブックにインポートされる
- [ ] 一覧にインポートされたファイルが表示される

#### 12.2 nb-export (ノートエクスポート)

**目的**: ノートをエクスポートできることを確認

**手順**:
1. `C-c n x` を実行
2. プロンプトに `1 /tmp/exported-note.md` と入力
3. ファイルがエクスポートされることを確認

**期待結果**:
- [ ] プロンプトが表示される: `nb export (id path):`
- [ ] ファイルが指定パスにエクスポートされる
- [ ] `ls /tmp/exported-note.md` でファイルが存在する

#### 12.3 nb-help (ヘルプ表示)

**目的**: ヘルプが表示されることを確認

**手順**:
1. `C-c n h` を実行
2. プロンプトで RET のみ押す（全体のヘルプ）

**期待結果**:
- [ ] プロンプトが表示される: `nb help (subcommand):`
- [ ] `*nb*` バッファに nb.sh のヘルプが表示される

**追加テスト**:
- [ ] サブコマンドのヘルプ: `add`
- [ ] サブコマンドのヘルプ: `bookmark`

#### 12.4 nb-archive (ノートブックアーカイブ)

**目的**: ノートブックをアーカイブできることを確認

**手順**:
1. テスト用ノートブックを作成: `nb notebooks add test-archive`
2. `C-c n r` を実行（`r` は archive のキー）
3. プロンプトに `test-archive` と入力

**期待結果**:
- [ ] プロンプトが表示される: `nb archive (notebook):`
- [ ] ノートブックがアーカイブされる
- [ ] `C-c n n --archived` でアーカイブ済みノートブックが表示される

---

### 13. エラーハンドリング

#### 13.1 存在しないノートの操作

**手順**:
1. `C-c n e` を実行
2. 存在しない ID `9999` を入力

**期待結果**:
- [ ] エラーメッセージが表示される: "Cannot find file for item 9999"

#### 13.2 不正なコマンド引数

**手順**:
1. `C-c n a` を実行
2. 不正な引数を入力（例: `--invalid-option`）

**期待結果**:
- [ ] nb コマンドのエラーメッセージが `*nb*` バッファに表示される

#### 13.3 nb コマンドが存在しない場合

**手順**:
1. `nb-executable` を存在しないパスに変更: `M-x set-variable RET nb-executable RET "/nonexistent" RET`
2. `C-c n RET` を実行

**期待結果**:
- [ ] エラーメッセージが表示される

**リセット**: `M-x set-variable RET nb-executable RET "nb" RET`

---

### 14. カスタマイズ

#### 14.1 nb-executable の変更

**手順**:
1. `M-x customize-variable RET nb-executable RET`
2. 値を変更（例: `/usr/local/bin/nb`）
3. 保存

**期待結果**:
- [ ] カスタマイズバッファが開く
- [ ] 値を変更できる
- [ ] 変更後も正常に動作する

#### 14.2 nb-output-buffer の変更

**手順**:
1. `M-x customize-variable RET nb-output-buffer RET`
2. 値を変更（例: `*my-nb-output*`）
3. コマンドを実行

**期待結果**:
- [ ] カスタマイズバッファが開く
- [ ] 値を変更できる
- [ ] 新しいバッファ名で出力が表示される

#### 14.3 キーバインドのカスタマイズ

**手順**:
1. init.el に以下を追加:
   ```elisp
   (setq nb-keymap-prefix (kbd "C-c b"))
   (nb-mode 1)
   ```
2. Emacs を再起動

**期待結果**:
- [ ] `C-c b RET` で nb コマンドが実行される
- [ ] 古いプレフィックス `C-c n` は無効になる

---

### 15. パフォーマンステスト

#### 15.1 大量のノートがある場合

**手順**:
1. 大量のノートを作成:
   ```bash
   for i in {1..100}; do nb add "Note $i" --content "Content $i"; done
   ```
2. `C-c n RET` を実行
3. スクロールして確認

**期待結果**:
- [ ] 一覧が問題なく表示される
- [ ] カラー表示が正しく適用される
- [ ] バッファのスクロールがスムーズ

#### 15.2 長いノート名の処理

**手順**:
1. 長いタイトルのノートを作成:
   ```bash
   nb add "$(printf 'A%.0s' {1..200})" --content "Long title test"
   ```
2. `C-c n RET` を実行

**期待結果**:
- [ ] 長いタイトルが表示される（切り捨てられる場合もあり）
- [ ] レイアウトが崩れない

---

## テスト完了後のクリーンアップ

```bash
# テスト用ノートブックを削除
nb use home
nb notebooks delete test-nb-el --force

# テスト用一時ファイルを削除
rm -f /tmp/test-import.txt /tmp/exported-note.md
```

---

## テスト結果サマリー

| カテゴリ | テスト数 | 成功 | 失敗 | スキップ |
|---------|---------|------|------|---------|
| 初期化 | 1 | | | |
| 基本コマンド | 3 | | | |
| 出力バッファ | 1 | | | |
| ブックマーク | 1 | | | |
| 検索 | 1 | | | |
| ノートブック管理 | 2 | | | |
| ファイル操作 | 3 | | | |
| 表示機能 | 2 | | | |
| TODO | 1 | | | |
| フォルダ | 1 | | | |
| Git統合 | 2 | | | |
| その他 | 4 | | | |
| エラーハンドリング | 3 | | | |
| カスタマイズ | 3 | | | |
| パフォーマンス | 2 | | | |
| **合計** | **30** | | | |

---

## 既知の問題・制限事項

記録欄:
-

---

## テスト実施情報

- **テスト実施日**: ____________________
- **テスト実施者**: ____________________
- **Emacs バージョン**: ____________________
- **nb.sh バージョン**: ____________________
- **OS**: ____________________

---

## 備考・コメント

自由記入欄:
